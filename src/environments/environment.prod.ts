/** Fallback se `conf.txt` faltar ou for invalido; em producao edite `conf.txt` na pasta publicada. */
export const environment = {
  production: true,
  defaultauth: 'api',
  apiUrl: 'https://localhost:7142'
};
